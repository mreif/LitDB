<!DOCTYPE html>
<html>
<head>
  <title>LiteraturDB - <%= yield(:title) %></title>
  	<%= stylesheet_link_tag    "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tag %>
</head>
<body>
	<div id="container">
		<div id="content" class="content">
			 <% if user_signed_in?%>
 -               <%= link_to "Logout", destroy_user_session_path, :method => :delete %>
 -            <% end %>
			<h1> <%= yield(:title) %></h1>
      		<%= yield %>
		</div>
		<div id="footer">
			
		</div>
	</div>
</body>
</html>
